import React, {Component} from 'react'

class CoordinatesButton extends Component {
    render() {
        return (
            <button>Coordinates Button</button>
        )
    }
}

export default CoordinatesButton